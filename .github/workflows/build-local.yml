name: build-local

on:
  push:
    branches:
      - main
  pull_request:
    paths:
      - src
      - .github/**/*
  workflow_dispatch:

jobs:
  build:
    name: Reuse build
    uses: ./.github/workflows/build.yml
